# CountLens — 写真から物体を検出して数量を計測するアプリ

写真を撮影／読み込むと、AIが対象物を自動検出し、数えたい対象を選んで数量を計測。
単価・金額の推定、撮影場所(GPS)・メモの記録、結果のExcel(.xlsx)出力ができます。

## ファイル構成

| ファイル | 内容 |
|---|---|
| `CountLens-PC.dc.html` | **PC（デスクトップ）版** — サイドバー型UI、ダーク/ライト切替、Webカメラ撮影対応 |
| `CountLens-iPhone.dc.html` | **iPhone版** — iPhoneフレーム内のダーク単一ビュー、標準カメラ撮影 |
| `CountLens-compare.dc.html` | 初期2案（1a ライト/ステップ・1b ダーク/フォーカス）の比較ビュー |
| `CounterApp.dc.html` | 比較ビューが読み込むモバイルアプリ本体 |
| `ios-frame.jsx` | iPhoneデバイスフレーム部品 |
| `support.js` | 実行ランタイム（各 .dc.html が読み込みます。削除・編集しないでください） |

> 各 `.dc.html` は同じフォルダ内の `support.js`（および iPhone系は `ios-frame.jsx`、比較版は `CounterApp.dc.html`）を必要とします。**フォルダごと**配置してください。

## 主な機能

- 📷 写真を撮影 / 🖼 ライブラリから選択
- 🟩 AIによる物体検出（対象物に外接する長方形フレーム＋番号バッジ）
- ✅ 対象物リストからチェックで計測対象を選択（複数可）
- ✏️ 手動補正（フレームのタップで削除、対象を選んで写真タップで追加）
- 🔢 カウント閾値（最小数量未満の対象を除外）
- 💴 推定単価・金額・推定総額（単価は手動編集可）
- 📍 撮影場所のGPS自動取得 / メモ
- 🗂 計測リスト（複数写真を蓄積）・対象物別集計
- 📊 Excel(.xlsx)出力（「対象物別集計」「記録明細」の2シート、罫線・色分け）
- 📂 保存済みExcelを開いて追記 → 同名で上書き保存（PCのChrome/Edge等は元ファイルへ直接上書き）

## 動作要件

- AI検出はこのプラットフォーム上で動作します（`window.claude` を利用）。
- カメラは **https** もしくは **localhost**（セキュアコンテキスト）で利用できます。
- Excelのセル装飾は `xlsx-js-style`、GPS読み取りは `exifr` をCDNから読み込みます（オンライン環境が必要）。

## 使い方（ローカルで開く場合）

カメラを使うには `file://` 直開きではなく、簡易サーバ経由で開いてください。

```bash
# このフォルダ内で
python3 -m http.server 8000
# ブラウザで http://localhost:8000/CountLens-PC.dc.html を開く
```
